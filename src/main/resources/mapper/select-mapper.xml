<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.home.jjh.hsk.mapper.SelectMapper">
    <!-- 유저데이타 개인 유저 데이타 -->
    <select id="getApiUser" resultType="com.home.jjh.hsk.model.userModel">
        SELECT uid ,
                displayname,
				email,
				bannerId,
				phoneNumber,
				photoUrl,
				providerId,
				tokenGoogle
        FROM `user`
        WHERE email = #{email}
	</select>

    <select id="getApiIntro" resultType="com.home.jjh.hsk.model.introModel">
		SELECT adId,
			dataAd,
			emgContent,
			emgTitle,
			lastAos,
			lastIos,
			upMsgAos,
			upMsgIos,
			upTitleAos,
			upTitleIos,
			verAos,
			verIos
		FROM `adintro`
	</select>

    <select id="getApiAd" resultType="com.home.jjh.hsk.model.adModel">
		SELECT adId,
			cnt,
			startDate,
			endDate,
			cntMax
		FROM `ad`
	</select>

	<select id="getApiAdDataItem" resultType="com.home.jjh.hsk.model.adDataModel">
		SELECT adId,
			adDetailUrl,
			adMsg,
			adNo,
			adStoreName,
			adUrl
		FROM `adData`
	</select>


    <select id="getApiEvent" resultType="com.home.jjh.hsk.model.eventModel">
		SELECT
			eventId,
			clickCount,
			endDate,
			eventContent,
			imgUrlContent,
			imgUrlMain,
			startDate
		FROM `event`
	</select>

	<select id="getApiItem" resultType="com.home.jjh.hsk.model.itemModel">
		SELECT
			shopId,
			shopItemDetailUrl,
			shopItemName,
			shopItemPrice_original,
			shopItemPriceSale,
			shopItemUrl,
			shopTitle,
			itemId
		FROM `item`
	</select>

	<select id="getApiShopsDataItem" resultType="com.home.jjh.hsk.model.itemModel">
		SELECT
			shopId,
			addInfoAsPhone,
			addInfoCeo,
			addInfoChangePay,
			addInfoEmail,
			addInfoInternetConfirmNumber,
			addInfoTranAutoType,
			addInfoTranCustomerCode,
			addInfoTranName,
			addInfoTransAddNumber,
			addInfoTrans_recallAdd,
			addInfoWorkingTime,
			basicAgeType,
			basicStyle,
			basicTitleWord,
			shopCountItemClick,
			shopCount_like,
			shopHomepage,
			shopMainImg,
			shopName
		FROM `shops`
	</select>

</mapper>